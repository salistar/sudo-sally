# ── Dependencies ──────────────────────────────────────────────
node_modules/
**/node_modules/

# ── Expo / React Native ───────────────────────────────────────
.expo/
**/.expo/
mobile/dist/
mobile/web-build/
*.tsbuildinfo

# ── Native projects (regenerated by `expo prebuild`) ──────────
mobile/android/
mobile/ios/

# ── Build artifacts ───────────────────────────────────────────
**/build/
**/.gradle/
**/.cxx/
artifacts/                       # local APK staging used by `gh release download`

# ── Secrets / environment (PUBLIC repo — never commit) ────────
.env
.env.*
!.env*.example
**/.env
**/.env.*
!**/.env*.example
*.keystore
*.jks
*.p8
*.p12
*.mobileprovision
**/google-services.json
**/GoogleService-Info.plist
deploy/.env.prod

# ── Built APKs (served via CI / GitHub Releases) ──────────────
*.apk
*.aab
landing/downloads/*.apk

# ── Logs / misc ───────────────────────────────────────────────
*.log
npm-debug.log*
yarn-error.log*

# ── OS / editors ──────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
java.lang.Thread
